# Flight Finder

## Setup
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Create a `.env` file with your Amadeus API credentials:

- AMADEUS_API_KEY=your_key

- AMADEUS_API_SECRET=your_secret

4. Run: `python flights.py`
